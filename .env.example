# Fireblocks 配置（将本文件复制为 .env 后填写实际值）

# 必填：Fireblocks API Key（从Console获取）
FIREBLOCKS_API_KEY=your_fireblocks_api_key

# 必填：Fireblocks API 私钥（PEM）。
# 建议使用多行形式；若必须单行，请用 \n 表示换行。
# 多行示例：
# FIREBLOCKS_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----
# ...
# -----END PRIVATE KEY-----
# 单行示例（注意转义换行符）：
FIREBLOCKS_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# 可选：Fireblocks API 基地址
# 生产：https://api.fireblocks.io
# 沙盒：https://sandbox-api.fireblocks.io
# FIREBLOCKS_API_BASE=https://api.fireblocks.io

# 代理服务端口（前端将通过 http://localhost:4000 调用）
FB_PROXY_PORT=4000

# 链下签名超时时间（毫秒）
FB_SIGN_TIMEOUT_MS=60000

# 可选：允许的前端来源（可在 server 层启用）
# CORS_ORIGIN=http://localhost:3000

# 运行环境/日志级别（可选）
# NODE_ENV=development
# LOG_LEVEL=info
